%h2
  = link_to l(:label_delay_plural), :action => :index
  &#187;
  =l(:label_delay_new)

- labelled_form_for @delay do |f|
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag l(:button_create)

